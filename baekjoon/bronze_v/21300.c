#include <stdio.h>

int main() {
    
    int b, m, w, c, s, water;
    
    scanf("%d %d %d %d %d %d", &b, &m, &w, &c, &s, &water);
    
    printf("%d", 5 * (b + m + w + c + s +water));
    
    return 0;
}
